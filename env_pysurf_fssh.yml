name: pysurf_fssh_test
channels:
  - conda-forge
  - defaults
dependencies:
  # Core scientific stack (from conda-forge, optimized + prebuilt)
  - python=3.12
  - numpy
  - scipy
  - matplotlib
  - h5py
  - netcdf4

  # pip section for extra packages
  - pip
  - pip:
      - pycolt
      - qctools
      - jinja2